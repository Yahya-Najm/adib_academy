{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build && npx prisma db push && npm run db:seed"
  },
  "deploy": {
    "startCommand": "npm start"
  }
}
